import { RemoveaccentPipe } from './removeaccent.pipe';

describe('RemoveaccentPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveaccentPipe();
    expect(pipe).toBeTruthy();
  });
});
